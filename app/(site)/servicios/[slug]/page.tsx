import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, ArrowRight, CheckCircle2, MessageCircle, Star } from "lucide-react";
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
import { Button } from "@/components/ui/button";
import { CONTACT, waLink } from "@/components/site/site-config";
import { ServiceInquiryFlow } from "@/components/site/ServiceInquiryFlow";
import { getService, services } from "@/content/services";

type PageProps = { params: Promise<{ slug: string }> };
const accents = { cyan: "border-cyan-200 bg-cyan-50 text-cyan-800", blue: "border-blue-200 bg-blue-50 text-blue-800", violet: "border-violet-200 bg-violet-50 text-violet-800", amber: "border-amber-200 bg-amber-50 text-amber-800" };

export function generateStaticParams() { return services.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: PageProps): Promise<Metadata> {
  const service = getService((await params).slug);
  return service ? { title: service.title, description: `${service.description} Consultá por WhatsApp a ServiTec.` } : { title: "Servicio no encontrado" };
}

export default async function ServiceDetailPage({ params }: PageProps) {
  const service = getService((await params).slug);
  if (!service) notFound();
  const Icon = service.icon;
  const message = `Hola ServiTec, quiero consultar por ${service.shortTitle}. Mi equipo es:`;

  return <>
    <section className="relative overflow-hidden border-b bg-gradient-to-br from-background via-background to-primary/[0.06]"><div aria-hidden className="absolute -left-32 top-0 size-80 rounded-full bg-secondary/10 blur-3xl" /><div aria-hidden className="absolute right-0 top-1/4 size-72 rounded-full bg-primary/10 blur-3xl" /><div className="container-page relative grid gap-10 py-14 sm:py-20 lg:grid-cols-[1fr_0.9fr] lg:items-center lg:py-24"><div><Link href="/servicios" className="inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground transition-colors hover:text-primary"><ArrowLeft className="size-4" /> Todos los servicios</Link><p className="eyebrow mt-7 rounded-full border border-primary/20 bg-primary/[0.06] px-3 py-1.5 text-[10px]">Especialistas en {service.shortTitle}</p><h1 className="mt-5 max-w-xl font-display text-5xl font-black leading-[0.93] tracking-[-0.055em] text-foreground sm:text-6xl lg:text-7xl">Reparación de {service.shortTitle}<span className="text-primary"> en Buenos Aires</span></h1><p className="mt-6 max-w-xl text-base leading-relaxed text-muted-foreground sm:text-lg">{service.intro}</p><div className="mt-8 flex flex-wrap gap-3"><Button asChild size="lg" className="bg-secondary text-secondary-foreground shadow-lg shadow-secondary/20 hover:bg-secondary/90"><a href="#consulta">Solicitar reparación <ArrowRight className="size-4" /></a></Button><Button asChild size="lg" variant="outline" className="border-whatsapp/40 bg-whatsapp/5 text-whatsapp hover:bg-whatsapp/10 hover:text-whatsapp"><a href={waLink(message)} target="_blank" rel="noopener noreferrer"><MessageCircle className="size-4" /> Cotizar por WhatsApp</a></Button></div><div className="mt-8 grid max-w-xl grid-cols-2 gap-3">{[["Reparación express", "Consultá por tu caso"], ["Garantía escrita", "En cada trabajo"], ["Ingresos", "Coordinados por WhatsApp"], ["Medios de pago", "Consultá alternativas"]].map(([title, value]) => <div key={title} className="rounded-xl border bg-card/80 p-3"><p className="text-[10px] font-semibold uppercase tracking-[0.15em] text-primary">{title}</p><p className="mt-1 text-sm font-bold">{value}</p></div>)}</div></div><div className="relative mx-auto w-full max-w-md"><div className={`relative aspect-[4/4.2] overflow-hidden rounded-[2rem] border bg-slate-950 p-5 shadow-2xl ${accents[service.accent]}`}><video className="absolute inset-0 size-full object-cover opacity-70" autoPlay muted loop playsInline preload="metadata" aria-hidden="true"><source src="/videos/frente-servitec.mp4" type="video/mp4" /></video><div aria-hidden className="absolute inset-0 bg-slate-950/55" /><div className="relative z-10 flex h-full flex-col justify-between rounded-[1.5rem] border border-white/10 bg-slate-950/65 p-6 text-white shadow-inner backdrop-blur-[1px]"><div className="flex items-start justify-between"><div className="grid size-14 place-items-center rounded-2xl bg-white/10"><Icon className="size-7" /></div><a href={CONTACT.reviewsUrl} target="_blank" rel="noopener noreferrer" className="rounded-2xl bg-white p-4 text-slate-900 shadow-xl transition-transform hover:scale-105"><p className="text-2xl font-black leading-none">4.9<span className="text-sm">/5</span></p><div className="mt-2 flex gap-0.5 text-amber-400">{Array.from({ length: 5 }).map((_, index) => <Star key={index} className="size-3 fill-current" />)}</div><p className="mt-2 text-[10px] font-semibold text-slate-500">Google Maps</p></a></div><div><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-secondary">Laboratorio ServiTec</p><h2 className="mt-3 font-display text-3xl font-black leading-none tracking-tight">Tu {service.shortTitle}, listo para volver a funcionar.</h2><p className="mt-4 text-sm leading-relaxed text-slate-300">Revisión técnica, información clara y reparación con garantía.</p><div className="mt-5 h-1.5 overflow-hidden rounded-full bg-white/20"><div className="h-full w-3/4 rounded-full bg-secondary" /></div></div></div></div><p className="mt-4 text-center text-xs text-muted-foreground">Valoración destacada en <a className="font-semibold text-primary hover:underline" href={CONTACT.reviewsUrl} target="_blank" rel="noopener noreferrer">Google Maps</a>.</p></div></div></section>
    <main className="bg-muted/35 py-12 sm:py-16 lg:py-20"><div id="consulta" className="scroll-mt-24"><ServiceInquiryFlow serviceName={service.shortTitle} repairs={service.repairs} models={service.models} /></div><section className="container-page mt-12 grid gap-4 md:grid-cols-3">{service.highlights.map(([title, description]) => <div key={title} className="rounded-2xl border bg-card p-5"><CheckCircle2 className="size-5 text-primary" /><h2 className="mt-3 font-display font-bold">{title}</h2><p className="mt-1 text-sm text-muted-foreground">{description}</p></div>)}</section><section className="container-page mx-auto mt-16 max-w-4xl"><div className="text-center"><p className="eyebrow text-primary">Preguntas frecuentes</p><h2 className="mt-2 font-display text-3xl font-black">Sobre {service.shortTitle}</h2></div><Accordion type="single" collapsible className="mt-6 rounded-2xl border bg-card px-4 sm:px-6">{service.faqs.map(([question, answer], index) => <AccordionItem key={question} value={`faq-${index}`}><AccordionTrigger className="text-left font-semibold hover:no-underline">{question}</AccordionTrigger><AccordionContent className="pb-4 text-muted-foreground">{answer}</AccordionContent></AccordionItem>)}</Accordion></section></main>
    <section className="border-t bg-background py-14"><div className="container-page flex flex-col items-center gap-4 text-center"><h2 className="font-display text-3xl font-black">¿Listo para revisar tu {service.shortTitle}?</h2><p className="max-w-xl text-sm text-muted-foreground">Escribinos el modelo y la falla. Te ayudamos a coordinar el ingreso del equipo.</p><Button asChild size="lg" className="bg-whatsapp text-whatsapp-foreground hover:bg-whatsapp/90"><a href={waLink(message)} target="_blank" rel="noopener noreferrer"><MessageCircle className="size-4" /> Cotizar por WhatsApp</a></Button></div></section>
  </>;
}
